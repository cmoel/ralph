name: Notify homebrew-tap

on:
  release:
    types: [published]

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - run: gh api repos/cmoel/homebrew-tap/dispatches -f event_type=ralph-release
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
